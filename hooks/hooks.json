{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/session-start.exe --db-path ${CLAUDE_PLUGIN_DATA}/kanban.db",
            "timeout": 10000
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/session-stop.exe --db-path ${CLAUDE_PLUGIN_DATA}/kanban.db",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
